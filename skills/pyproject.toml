[project]
name = "airush-skills"
version = "0.0.0"
description = "AIRush skill services (scaffold placeholder, framework lands in spec-1.9)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["airush_skills"]
